# Example .env file for OPML Feed Provider
# Copy this file to .env and fill in your values if needed

# Path to the OPML file (default: feedls.opml in the application directory)
# OPML_FILE_PATH=feedls.opml

# Set to true to enable verbose logging
# VERBOSE_LOGGING=false
